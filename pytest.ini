[pytest]
pythonpath = src
testpaths = tests
markers =
    integration: marks tests that perform real Docker operations (deselect with '-m "not integration"')
    timeout: mark tests with a timeout value (documentary only unless pytest-timeout plugin is installed)
