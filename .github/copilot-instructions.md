effective potato aims to be an mcp to allow for callable executions against a sandbox'd ubuntu 24.04 docker container

Write effective python3 code that is backed by pytest 

Use the venv in $ROOT_PROJECT_DIR/venv

When updating the codebase, re-run the test suite to validate no breakage, and attempt to repair test case failures 
